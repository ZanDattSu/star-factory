type: object
required:
  - code
  - message
properties:
  code:
    type: integer
    description: HTTP-код ошибки
    example: 422
  message:
    type: string
    description: Ошибка бизнес-валидации запроса
    example: "Validation Error: part_uuids must contain at least one UUID"
