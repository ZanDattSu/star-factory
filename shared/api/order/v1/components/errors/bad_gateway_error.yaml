type: object
required:
  - code
  - message
properties:
  code:
    type: integer
    description: HTTP-код ошибки
    example: 502
  message:
    type: string
    description: Ошибка при вызове внешнего сервиса (Inventory/Payment)
    example: "Bad Gateway: Inventory service is unavailable"
