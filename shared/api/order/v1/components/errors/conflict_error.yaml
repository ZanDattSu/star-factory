type: object
required:
  - code
  - message
properties:
  code:
    type: integer
    description: HTTP-код ошибки
    example: 409
  message:
    type: string
    description: Конфликт состояния (например, попытка отменить уже оплаченный заказ)
    example: "Conflict: Cannot cancel a paid order"
